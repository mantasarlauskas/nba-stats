const hostUrl = 'http://localhost:8000/';

export const pointsLeaders = `${hostUrl}pointsleaders`;
export const reboundsLeaders = `${hostUrl}reboundsleaders`;
export const assistsLeaders = `${hostUrl}assistsleaders`;
export const blocksLeaders = `${hostUrl}blocksleaders`;
export const stealsleaders = `${hostUrl}stealsleaders`;
